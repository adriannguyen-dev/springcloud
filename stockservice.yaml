server:
  port: 8900

spring:
  application:
    name: StockService

  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        register: true

management:
  tracing:
    sampling:
      probability: 1.0

otlp:
  tracing:
    endpoint: http://localhost:9411/api/v2/spans

